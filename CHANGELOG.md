# Changelog

## 未发布

- 修复无关 Protobuf 字符串和损坏元数据导致整批扫描中断的问题，单文件失败会保留诊断并继续扫描。
- 按需读取 djmd 第一包，共享容器读取上下文，并限制元数据大小和解析深度。
- 修复 Web 取消按钮状态，增加实际进度、取消后未执行数量和扫描结果失效保护。
- 复制改为临时文件写入、校验后发布；取消或失败时清理临时数据，保护已存在的目标文件。
- 视频与伴随文件共享冲突编号，目录内建立伴随文件索引；消除 Web 重复遍历目录并支持枚举阶段取消。
- 文件列表增加分页和搜索防抖；新增真实 JavaScript 行为测试及发布包自检。
- 修复 Windows 无控制台发布包的日志初始化异常，并统一打包自检日志编码。
- 固定 Ruff 版本与规则集，保证本地和 CI 使用相同的代码检查基线。

## 2.0.0（2026-08-30）

- Web 工作台结束预览并成为正式发布界面。
- 正式发布包精简为 Windows x64 与 macOS Apple Silicon 的 Web 桌面版，不再构建 CLI 和 Qt GUI 安装包。
- 包含 2.0.0-preview.1 至 preview.3 的全部功能、界面优化与稳定性修复。

## 2.0.0-preview.3（2026-08-30）

- 将 Web 工作台重构为固定高度应用壳，窗口本身不再滚动，文件表格和整理设置分别使用局部滚动区域。
- 合并重复的统计与筛选入口；窄窗口改用整理设置抽屉，低高度窗口自动进入紧凑布局。
- 新增固定表头、任务进度条、拖放遮罩、系统主题适配和常用桌面快捷键。
- 报告格式改为应用内对话框选择，移除语义含糊的浏览器确认框。
- 修复 Web 工作台重复拖拽可能并发扫描、切换目录残留旧筛选以及目录状态显示滞后的问题。
- 修复 pywebview 6.2 递归暴露原生窗口导致桥接初始化栈溢出、文件夹拖放再次失效的问题。
- 修复伴随文件未遵守冲突策略、“标记为失败”被错误统计为跳过的问题。
- 关闭 Web 窗口时会取消剩余后台整理，并补充动态状态与切换控件的无障碍语义。

## 2.0.0-preview.2（2026-08-29）

- 修复 Web 工作台拖入素材文件夹不生效的问题，改用 pywebview Python DOM `drop` 事件读取完整本地路径。
- 增加拖拽路径桥接回归测试，并在普通浏览器中显示明确的本地路径限制提示。

## 2.0.0-preview.1（2026-08-29）

- 新增 pywebview 原生 Web 工作台入口 `dji-color-web`。
- 新增 Python application service、任务状态/进度/取消协议和结构化 Web DTO。
- 原型页面接入真实扫描、整理计划、执行、报告导出、manifest 撤销和 HLG HDR 统计。
- 新增 Web 服务层端到端验收测试与 Web 版 PyInstaller spec。

## 1.1.0

- 新增结构化 QuickTime `ColorGammaSxS` 标签读取，优先使用文件内明确色彩模式。
- 新增 `Rec.2100 HLG（HDR）` 识别、报告、GUI 筛选和 `hlg/` 分类目录。
- 增加多证据置信度与冲突保护；冲突文件不会被自动整理。
- 优化单页 GUI 的扫描、筛选和整理流程，并补充元数据回归测试。

## 1.0.0-dev

- 重构为核心库、CLI 和 GUI 三层结构。
- 新增原生 MP4/MOV `djmd` 读取器，降低对 `ffmpeg/ffprobe` 的依赖。
- 新增 `dji-color scan`、`organize`、`undo`、`version` 命令。
- 新增前缀、移动、复制、manifest 和 undo 流程。
- 新增 PySide6 优先、PyQt5 兼容的 GUI 工作台。
- 新增分类、MP4 reader、计划、manifest、GUI 冒烟测试。
