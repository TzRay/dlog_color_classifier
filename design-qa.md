# GUI Design QA

## 当前设计方向

GUI 面向“识别速度快、功能简单”的使用场景，不再采用侧栏和三步向导。当前版本使用单页流程：

1. 选择、拖入或粘贴 DJI 视频目录。
2. 自动识别并显示六项紧凑统计（含 Rec.2100 HLG HDR）。
3. 直接选择复制、移动或添加前缀。
4. 在最终确认后执行整理。

文件明细、高级设置和运行日志默认折叠，避免低频信息干扰主操作。

## 视觉验收

- 默认窗口：1120 × 780。
- 最小窗口：900 × 660。
- 浅灰工作区、白色卡片和 DJI 红主操作形成稳定层级。
- 主内容最大宽度为 1080px，超宽屏不会无限拉伸。
- 默认复制方式同时使用勾选、文字和红色边框，不只依赖颜色。
- 路径框、筛选框、表格均设置了辅助功能名称和可见焦点状态。
- 文件明细与整理操作互相切换，避免表格展开后挤压主按钮。
- 明细表默认只显示状态、文件名、色彩模式和所在文件夹；完整路径通过悬浮提示提供。

本轮本地验收生成了 `12-final-gui.png` 和 `13-final-details.png`，截图属于工作过程证据，不作为发布资产提交。

## 交互与状态验收

- 选择目录或拖入目录后只启动一次扫描；手工粘贴路径后按 Enter 扫描。
- 扫描期间锁定路径、范围和整理选项，旧扫描结果不能覆盖新目录。
- 重新识别会清空旧筛选条件。
- 扫描完成后无需页面跳转即可执行默认复制整理。
- 取消最终确认会丢弃临时计划并恢复可筛选的结果。
- 执行完成后旧结果不能因切换整理方式而再次执行。
- 成功、跳过和失败分别统计；跳过项在明细中保持“跳过”状态。
- Undo 使用 manifest 中记录的根目录，不依赖之后编辑的路径框。
- 真实 3 文件目录的扫描、计划生成及临时目录复制和 manifest 写入均已通过。

## 兼容性

- 保留 PySide6 / PyQt5 双绑定。
- 保留 `path_edit`、`mode_combo`、计划生成和执行方法，兼容现有测试与验收脚本。
- 统一行模型继续保证伴随文件与 `PlanItem` 对齐。

final result: passed
